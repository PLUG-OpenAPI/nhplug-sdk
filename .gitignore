__pycache__/

# 종목마스터 원본(.mst)은 저장소에 넣지 않는다.
# - 매일 갱신되는 데이터라 커밋하면 즉시 낡는다
# - 포털이 배포 정본. 파서가 @url 로 자동 다운로드하거나 사용자가 직접 받는다
instruments/.cache/
*.mst
*.pyc
.env
*.env
!.env.example
.venv/
.uv/

# 도메인(www.nhplug.com)이 명세 정본(SSOT). docs 미러는 fetch_docs.py 로 받으며 커밋하지 않는다.
# (docs/README.md 만 커밋 유지)
docs/llms.txt
docs/common/
docs/krstock/
docs/gbstock/
docs/krfuture/
docs/gbfuture/
docs/krbond/
docs/krgold/
